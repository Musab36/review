{{! A Component responsible for listing properties to be rated on the index template}}
{{! This Component also shows and hides images}}
<li>
  {{#link-to 'review' review.id}}{{review.restaurant}} in {{review.city}}. Author: {{review.author}}{{/link-to}}
  {{#if imageIsShown}}
  <p><img src={{review.image}} {{action 'hideImage'}}></p>
  {{else}}
  <a her="#" {{action 'showImage'}}><span class="show">Show Image</span></a>
  {{/if}}
</li>
